# PUBLIC_INTERFACE
# Example environment variables for the Integration Frontend

# Absolute base URL to the backend ORIGIN (e.g., https://localhost:3001). Do NOT include any path like '/docs'.
# This must match the backend's BACKEND_PUBLIC_BASE_URL origin.
NEXT_PUBLIC_BACKEND_URL=

# Absolute base URL to the frontend (used to compute return_url when starting OAuth)
# Falls back to window.location.origin at runtime if not provided.
NEXT_PUBLIC_FRONTEND_BASE_URL=
