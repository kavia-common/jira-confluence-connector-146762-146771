# PUBLIC INTERFACE: Frontend environment template
# Set the backend base URL if the frontend is served from a different origin.
# If omitted, the frontend will call the backend via same origin.
# Example (local dev):
# NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
# Example (Kavia internal env - adjust to your backend URL):
# NEXT_PUBLIC_BACKEND_URL=https://vscode-internal-40486-beta.beta01.cloud.kavia.ai:3001

NEXT_PUBLIC_BACKEND_URL=
