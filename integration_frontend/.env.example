# Public environment variables used by the Next.js frontend
#
# Base URL of the backend API. MUST be an absolute, public URL when running in cloud.
# Do NOT use localhost in cloud environments; OAuth redirects will fail.
# Example: https://vscode-internal-xxxxx.cloud.kavia.ai:3001
NEXT_PUBLIC_BACKEND_URL=https://your-backend.example.com
