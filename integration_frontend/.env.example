# PUBLIC variables exposed to the browser must be prefixed with NEXT_PUBLIC_
# Backend base URL. If omitted, the frontend will call the backend via same origin.
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
