# Frontend Environment Example for Confluence OAuth

# Optional: Backend base URL to forward OAuth callbacks
# e.g., https://vscode-internal-27312-beta.beta01.cloud.kavia.ai:3001
NEXT_PUBLIC_BACKEND_URL=

# Confluence OAuth client configuration (server-side preferred)
CONFLUENCE_CLIENT_ID=
# Likely callback for cloud preview:
# https://vscode-internal-27312-beta.beta01.cloud.kavia.ai:4000/api/oauth/callback/confluence
CONFLUENCE_OAUTH_REDIRECT_URI=

# Fallbacks for environments where only NEXT_PUBLIC_* is available (preview/static)
NEXT_PUBLIC_CONFLUENCE_CLIENT_ID=
NEXT_PUBLIC_CONFLUENCE_OAUTH_REDIRECT_URI=

# Optional: Atlassian auth base override (defaults to https://auth.atlassian.com)
# JIRA_AUTH_BASE=
