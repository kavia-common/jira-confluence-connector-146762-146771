# Base URL of the backend API; embedded at build time for client-side usage.
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Optional: for display/diagnostics only. Backend owns redirect_uri; frontend should NOT send it.
# If you need to reference the expected redirect in UI, set this accordingly.
NEXT_PUBLIC_JIRA_REDIRECT_URI=https://vscode-internal-36721-beta.beta01.cloud.kavia.ai:3001/auth/jira/callback
