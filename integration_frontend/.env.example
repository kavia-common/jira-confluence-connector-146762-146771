# Public environment variables used by the frontend (client-side safe)
# Set this to the backend base URL so the frontend can start OAuth flows.
# Example:
# NEXT_PUBLIC_BACKEND_URL=https://vscode-internal-28816-beta.beta01.cloud.kavia.ai:3001

# Base URL of the backend FastAPI service (no trailing slash)
NEXT_PUBLIC_BACKEND_URL=
