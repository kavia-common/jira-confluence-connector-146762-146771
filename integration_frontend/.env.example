# Public environment variables used by the Next.js frontend

# Base URL of the backend API. If omitted, the app may default to same-origin or localhost.
# Example: https://your-backend.example.com
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
