# Frontend Environment Variables

# When set, frontend will call backend at this base URL (absolute).
# When omitted, frontend assumes same-origin.
# Example (replace host): https://vscode-internal-XXXXX-beta.beta01.cloud.kavia.ai:3001
NEXT_PUBLIC_BACKEND_URL=
