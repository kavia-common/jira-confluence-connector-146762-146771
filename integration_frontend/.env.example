# Frontend environment variables for Jira/Confluence OAuth (Next.js)
# Note: Prefer setting server-only envs on the backend. The NEXT_PUBLIC_* here are used by the frontend
# when initiating OAuth directly and for computing callback URLs in cloud preview environments.

# Backend base URL; when set, frontend will start OAuth at {BACKEND}/auth/{provider}/login
NEXT_PUBLIC_BACKEND_URL=https://your-backend.example.com

# Frontend base URL used to compute default return_url hints and UI redirects
NEXT_PUBLIC_FRONTEND_BASE_URL=https://your-frontend.example.com

# Jira OAuth (frontend fallback) - If backend initiates login, these are not required on frontend.
NEXT_PUBLIC_JIRA_OAUTH_CLIENT_ID=your-atlassian-client-id
NEXT_PUBLIC_JIRA_OAUTH_CLIENT_SECRET=not-used-on-frontend
NEXT_PUBLIC_JIRA_OAUTH_REDIRECT_URI=https://your-frontend.example.com/api/oauth/callback/jira
