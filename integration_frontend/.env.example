# Public environment variables for the frontend
# If omitted, the app will call the backend via same-origin relative paths.
# For cloud preview, set this to the running backend URL.
NEXT_PUBLIC_BACKEND_URL=https://vscode-internal-22588-beta.beta01.cloud.kavia.ai:3001
