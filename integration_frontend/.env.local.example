# Copy to .env.local and adjust as needed for local development.
# If unset, frontend will call backend via same-origin.
# For cloud preview, set to the public backend URL.
NEXT_PUBLIC_BACKEND_URL=https://vscode-internal-22588-beta.beta01.cloud.kavia.ai:3001
